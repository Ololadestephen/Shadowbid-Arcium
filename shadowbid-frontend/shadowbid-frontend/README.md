# ShadowBid Frontend

This nested folder is not the active ShadowBid frontend used by the deployed app.

The active React/Vite frontend is located at:

```bash
../
```

For the real frontend documentation, see:

```bash
../README.md
```

ShadowBid's active frontend supports Solana wallet connection, auction creation, encrypted bidding, Arcium finalization, status refresh, and losing-bid refunds.
